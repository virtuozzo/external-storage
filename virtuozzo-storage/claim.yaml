kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: vz-claim
  annotations:
    volume.beta.kubernetes.io/storage-class: "virtuozzo-storage"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
